public enum GameStatus {
    /*
    Create an enum named GameStatus that represents various possible statuses for a game.

    Steps:

    1. Create an enum named GameStatus.

    2. Add enum constants for NOT_STARTED, IN_PROGRESS, PAUSED, and COMPLETED.

    3. Create a class named GameStatusesExample with a main method.

    4. In the main method, print the GameStatus enum constants using a loop.

    5. Also, create a variable of type GameStatus and assign it the label PAUSED.
     */

    NOT_STARTED,
    IN_PROGRESS,
    PAUSED,
    COMPLETED
}
