package exercises;

public class Person {
    /*
    Create a class with fields using different access modifiers and try to access them from another class.

    Steps:

    1. Create a class named Person.

    2. Inside the Person class, create fields (e.g. name, age, address, ssn) with different access modifiers: public, protected, private, and one without a modifier (default).

    3. Create another class named Main.

    4. In the main method of the Main class, create an instance of the Person class and try to access its fields.

    5. Which field can you access? Which fields can’t you access?


         */

    public String name;
    protected int age ;
    private int height;
    String address;
}
